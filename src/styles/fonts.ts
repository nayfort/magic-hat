import { TFonts } from "@/styles/types";
import { sf } from "@/utils";

export const fonts: TFonts = {
  caption: {
    fontSize: sf(14),
    fontWeight: "400",
  },
  default: {
    fontSize: sf(16),
    fontWeight: "400",
  },
  subtitle: {
    fontSize: sf(20),
    fontWeight: "600",
  },
  title: {
    fontSize: sf(28),
    fontWeight: "bold",
  },
};
