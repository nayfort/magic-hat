import { TFonts } from "@/styles/types";
import { sf } from "@/utils";

export const fonts: TFonts = {
  caption: {
    fontSize: sf(14),
    fontWeight: "regular",
  },
  default: {
    fontSize: sf(16),
    fontWeight: "regular",
  },
  subtitle: {
    fontSize: sf(20),
    fontWeight: "semibold",
  },
  title: {
    fontSize: sf(28),
    fontWeight: "bold",
  },
};
